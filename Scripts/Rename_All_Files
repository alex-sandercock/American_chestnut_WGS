# Rename all *.txt to *.text
for f in *.txt; do #Designates what file extension to look for
    mv -- "$f" "${f%.txt}.text" #restates what file extension to look for, and adds what extension you want instead
done